{
  "name": "@ajwdmedia/pdf-to-png-converter",
  "version": "3.3.1",
  "description": "Fork of pdf-to-png-converter that adds resolution controls.",
  "homepage": "https://github.com/ajwdmedia/pdf-to-png-converter#readme",
  "bugs": {
    "url": "https://github.com/ajwdmedia/pdf-to-png-converter/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ajwdmedia/pdf-to-png-converter.git"
  },
  "license": "MIT",
  "author": "AJWD Media <hello@ajwd.media>",
  "main": "out/index.js",
  "types": "out/index.d.ts",
  "files": [
    "/out"
  ],
  "scripts": {
    "prebuild": "npm run clean",
    "build": "tsc --pretty --project tsconfig.prod.json",
    "build:test": "tsc --pretty --noEmit --project tsconfig.test.json",
    "clean": "npx rimraf ./out ./coverage ./test-results",
    "docker:build": "docker build --compress -t test-pdf-to-png-converter .",
    "predocker:run": "npm run clean",
    "docker:run": "docker run --rm -it -v $PWD/test-results:/usr/pkg/test-results test-pdf-to-png-converter",
    "predocker:test": "npm run build:test",
    "docker:test": "vitest run",
    "license-checker": "npx license-checker --production --onlyAllow 'MIT; MIT OR X11; BSD; ISC; Apache-2.0; Unlicense'",
    "lint": "eslint .",
    "lint:fix": "npm run lint -- --fix",
    "pretest": "npm run clean",
    "test": "vitest run --coverage",
    "test:docker": "npm run docker:build && npm run docker:run"
  },
  "dependencies": {
    "canvas": "^2.11.2",
    "pdfjs-dist": "^4.5.136"
  },
  "devDependencies": {
    "@types/node": "^20.16.2",
    "@typescript-eslint/eslint-plugin": "^8.3.0",
    "@typescript-eslint/parser": "^8.3.0",
    "@vitest/coverage-v8": "^2.0.5",
    "eslint": "^9.9.1",
    "png-visual-compare": "^1.2.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
